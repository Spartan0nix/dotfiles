- name: Retrieve host PATH if running WSL
  get_vscode_path:
  register: windows_host_path

- name: Set fact for VSCODE_PATH
  set_fact:
    WSL_VSCODE_PATH: "{{ windows_host_path.path }}"
  when: windows_host_path.found